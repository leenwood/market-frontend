import { Component, inject, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { ProductCardComponent } from '../../shared/product-card.component';import { CatalogService } from '../../core/services/catalog.service';import { Product } from '../../core/models';
@Component({standalone:true,imports:[FormsModule,ProductCardComponent],template:`<h1 class="mb-4 text-2xl font-bold">Поиск</h1><div class="grid gap-4 md:grid-cols-[280px_1fr]"><aside class="card space-y-3 p-4"><input class="input" placeholder="Название или бренд" [(ngModel)]="q" (ngModelChange)="run();complete()"><div class="text-xs text-slate-500">@for(s of suggestions();track s){<button class="mr-2 underline" (click)="q=s;run()">{{s}}</button>}</div><select class="input" [(ngModel)]="category" (change)="run()"><option value="all">Все категории</option><option value="electronics">Электроника</option><option value="home">Дом</option><option value="sport">Спорт</option><option value="books">Книги</option></select><select class="input" [(ngModel)]="sort" (change)="run()"><option value="rel">По релевантности</option><option value="price">По цене</option><option value="popular">По популярности</option></select><label class="flex gap-2 text-sm"><input type="checkbox" [(ngModel)]="inStock" (change)="run()">В наличии</label></aside><section class="grid grid-cols-2 gap-3 md:grid-cols-3">@for(p of results();track p.id){<app-product-card [product]="p"/>}@empty{<p class="text-slate-500">Ничего не найдено</p>}</section></div>`})
export class SearchPage{api=inject(CatalogService);q='';category='all';sort='rel';inStock=false;results=signal<Product[]>([]);suggestions=signal<string[]>([]);constructor(){this.run()}run(){this.api.search(this.q,{category:this.category,sort:this.sort,inStock:this.inStock}).subscribe(v=>this.results.set(v))}complete(){this.q?this.api.autocomplete(this.q).subscribe(v=>this.suggestions.set(v)):this.suggestions.set([])}}
