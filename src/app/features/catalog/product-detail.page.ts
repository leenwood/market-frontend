import { Component, inject, signal } from '@angular/core';import { ActivatedRoute, RouterLink } from '@angular/router';import { CatalogService } from '../../core/services/catalog.service';import { CartService } from '../../core/services/cart.service';import { AuthService } from '../../core/services/auth.service';import { Product } from '../../core/models';
@Component({standalone:true,imports:[RouterLink],template:`@if(product();as p){<section class="grid gap-6 md:grid-cols-2"><img [src]="p.image" class="w-full rounded-2xl object-cover"><div><p class="text-sm text-slate-500">{{p.brand}} · ★ {{p.rating.toFixed(1)}} · {{p.reviews}} отзывов</p><h1 class="mt-2 text-3xl font-bold">{{p.title}}</h1><p class="mt-4 text-2xl font-bold">{{p.price}} ₽</p><p class="mt-4 text-slate-700">{{p.description}}</p><div class="mt-4 flex gap-2">@for(a of attr(p);track a[0]){<span class="rounded-full bg-slate-100 px-3 py-1 text-sm">{{a[0]}}: {{a[1]}}</span>}</div><button class="btn mt-6" (click)="cart.add(p)">{{cart.qty(p.id)?'Уже в корзине: '+cart.qty(p.id):'В корзину'}}</button>@if(auth.user()){<button class="ml-3 rounded-xl border px-4 py-2">В избранное</button>}<p class="mt-4"><a routerLink="/cart" class="underline">Перейти в корзину</a></p></div></section>} @else {<p>Товар не найден</p>}`})
export class ProductDetailPage{api=inject(CatalogService);route=inject(ActivatedRoute);cart=inject(CartService);auth=inject(AuthService);product=signal<Product|null>(null);constructor(){this.api.getProduct(this.route.snapshot.paramMap.get('id')!).subscribe(v=>this.product.set(v))}attr(p:Product){return Object.entries(p.attributes)}}
