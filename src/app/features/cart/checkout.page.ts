import { Component, inject } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router } from '@angular/router';import { CartService } from '../../core/services/cart.service';import { OrdersService } from '../../core/services/orders.service';
@Component({standalone:true,imports:[FormsModule],template:`<h1 class="mb-4 text-2xl font-bold">Оформление заказа</h1><form class="grid gap-4 md:grid-cols-[1fr_360px]" (ngSubmit)="submit()"><section class="card space-y-3 p-4"><input required class="input" name="city" [(ngModel)]="city" placeholder="Город"><input required class="input" name="street" [(ngModel)]="street" placeholder="Улица"><input required class="input" name="zip" [(ngModel)]="zip" placeholder="Индекс"><select class="input" name="pay"><option value="card">Карта</option></select></section><aside class="card h-fit p-4"><p class="mb-2 font-bold">Состав заказа</p>@for(i of cart.items();track i.product.id){<p class="text-sm">{{i.product.title}} × {{i.qty}}</p>}<p class="mt-4 text-lg font-bold">{{cart.total()}} ₽</p><button class="btn mt-4 w-full" [disabled]="!cart.items().length">Подтвердить заказ</button></aside></form>`})
export class CheckoutPage{cart=inject(CartService);orders=inject(OrdersService);router=inject(Router);city='';street='';zip='';submit(){if(!this.cart.items().length)return;const o=this.orders.create(this.cart.items(),{city:this.city,street:this.street,zip:this.zip});this.cart.clear();this.router.navigate(['/orders',o.id])}}
