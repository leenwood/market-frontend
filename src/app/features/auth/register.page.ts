import { Component, inject, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router, RouterLink } from '@angular/router';import { AuthService } from '../../core/services/auth.service';
@Component({standalone:true,imports:[FormsModule,RouterLink],template:`<form class="card mx-auto max-w-md space-y-3 p-6" (ngSubmit)="submit()"><h1 class="text-2xl font-bold">Регистрация</h1><input class="input" required name="name" [(ngModel)]="name" placeholder="Имя"><input class="input" required name="email" type="email" [(ngModel)]="email" placeholder="Email"><input class="input" required name="password" type="password" [(ngModel)]="password" placeholder="Пароль"><input class="input" required name="confirm" type="password" [(ngModel)]="confirm" placeholder="Повторите пароль">@if(error()){<p class="text-sm text-red-600">{{error()}}</p>}<button class="btn w-full" [disabled]="loading()">{{loading()?'Регистрация...':'Зарегистрироваться'}}</button><a routerLink="/auth/login" class="text-sm underline">Уже есть аккаунт?</a></form>`})
export class RegisterPage{auth=inject(AuthService);router=inject(Router);name='';email='';password='';confirm='';error=signal('');loading=signal(false);submit(){if(this.password!==this.confirm){this.error.set('Пароли не совпадают');return}this.error.set('');this.loading.set(true);this.auth.register(this.name,this.email,this.password).subscribe({next:()=>this.router.navigateByUrl('/'),error:(err:any)=>{this.error.set(err?.error?.error??'Ошибка регистрации');this.loading.set(false)}})}}
