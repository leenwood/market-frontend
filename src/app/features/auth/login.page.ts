import { Component, inject } from '@angular/core';import { FormsModule } from '@angular/forms';import { ActivatedRoute, Router, RouterLink } from '@angular/router';import { AuthService } from '../../core/services/auth.service';
@Component({standalone:true,imports:[FormsModule,RouterLink],template:`<form class="card mx-auto max-w-md space-y-3 p-6" (ngSubmit)="submit()"><h1 class="text-2xl font-bold">Вход</h1><input class="input" required name="email" type="email" [(ngModel)]="email" placeholder="Email"><input class="input" required name="password" type="password" [(ngModel)]="password" placeholder="Пароль"><button class="btn w-full">Войти</button><a routerLink="/auth/register" class="text-sm underline">Нет аккаунта? Зарегистрируйтесь</a></form>`})
export class LoginPage{auth=inject(AuthService);router=inject(Router);route=inject(ActivatedRoute);email='demo@example.com';password='demo';submit(){this.auth.login(this.email,this.password);this.router.navigateByUrl(this.route.snapshot.queryParamMap.get('redirect')||'/')}}
