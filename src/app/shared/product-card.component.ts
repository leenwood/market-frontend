import { Component, Input, inject } from '@angular/core';import { RouterLink } from '@angular/router';import { Product } from '../core/models';import { CartService } from '../core/services/cart.service';
@Component({selector:'app-product-card',standalone:true,imports:[RouterLink],template:`<article class="card overflow-hidden"><a [routerLink]="['/products',product.id]"><img [src]="product.image" [alt]="product.title" class="aspect-square w-full object-cover"><div class="p-3"><p class="line-clamp-2 min-h-10 text-sm font-medium">{{product.title}}</p><p class="text-xs text-slate-500">{{product.brand}} · ★ {{product.rating.toFixed(1)}}</p><p class="mt-2 font-bold">{{product.price}} ₽</p></div></a><button class="mx-3 mb-3 w-[calc(100%-1.5rem)] rounded-xl border px-3 py-2 text-sm" (click)="cart.add(product)">{{cart.qty(product.id)?'В корзине: '+cart.qty(product.id):'В корзину'}}</button></article>`})
export class ProductCardComponent{@Input({required:true}) product!:Product;cart=inject(CartService)}
